{
  "private": true,
  "name": "comment-viewer-do",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "types": "wrangler types",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "prettier": "^3.8.0",
    "vitest": "^3.2.x",
    "wrangler": "^4.58.0"
  }
}
